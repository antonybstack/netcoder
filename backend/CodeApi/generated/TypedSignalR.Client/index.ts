/* THIS (.ts) FILE IS GENERATED BY TypedSignalR.Client.TypeScript */
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import type { HubConnection, IStreamResult, Subject } from '@microsoft/signalr';
import type { IIntellisenseHub } from './CodeApi.Hubs';
import type { IntellisenseTextRequest } from '../CodeApi.Models.Intellisense.Requests';


// components

export type Disposable = {
    dispose(): void;
}

export type HubProxyFactory<T> = {
    createHubProxy(connection: HubConnection): T;
}

export type ReceiverRegister<T> = {
    register(connection: HubConnection, receiver: T): Disposable;
}

type ReceiverMethod = {
    methodName: string,
    method: (...args: any[]) => void
}

class ReceiverMethodSubscription implements Disposable {

    public constructor(
        private connection: HubConnection,
        private receiverMethod: ReceiverMethod[]) {
    }

    public readonly dispose = () => {
        for (const it of this.receiverMethod) {
            this.connection.off(it.methodName, it.method);
        }
    }
}

// API

export type HubProxyFactoryProvider = {
    (hubType: "IIntellisenseHub"): HubProxyFactory<IIntellisenseHub>;
}

export const getHubProxyFactory = ((hubType: string) => {
    if(hubType === "IIntellisenseHub") {
        return IIntellisenseHub_HubProxyFactory.Instance;
    }
}) as HubProxyFactoryProvider;

export type ReceiverRegisterProvider = {
}

export const getReceiverRegister = ((receiverType: string) => {
}) as ReceiverRegisterProvider;

// HubProxy

class IIntellisenseHub_HubProxyFactory implements HubProxyFactory<IIntellisenseHub> {
    public static Instance = new IIntellisenseHub_HubProxyFactory();

    private constructor() {
    }

    public readonly createHubProxy = (connection: HubConnection): IIntellisenseHub => {
        return new IIntellisenseHub_HubProxy(connection);
    }
}

class IIntellisenseHub_HubProxy implements IIntellisenseHub {

    public constructor(private connection: HubConnection) {
    }

    public readonly requestCompletions = async (payload: IntellisenseTextRequest): Promise<void> => {
        return await this.connection.invoke("RequestCompletions", payload);
    }
}


// Receiver

